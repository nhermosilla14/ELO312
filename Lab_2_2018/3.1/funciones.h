#ifndef FUNCIONES.H
#define FUNCIONES.H
/*
 * Retorna la suma de los parámetros ingresados (por valor)
 */
int suma(int x1, int x2, int x3, int x4, int x5, int x6);

/*
 * Retorna la suma de los parámetros ingresados (por referencia)
 */
int suma2(int *x1, int *x2, int *x3, int *x4, int *x5, int *x6);

/*
 * Retorna la suma de los parámetros ingresados (por valor)
 * en forma de long int
 */
int suma3(int x1, int x2, int x3, int x4, int x5, int x6);

#endif